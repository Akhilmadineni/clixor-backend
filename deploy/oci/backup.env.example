# Local-staging reference only. Production derives this file from the
# postgres_env Vault bundle; do not create a separate backup Vault secret.
POSTGRES_DB=clixor
POSTGRES_USER=clixor
POSTGRES_PASSWORD=REPLACE_WITH_SAME_POSTGRES_PASSWORD
