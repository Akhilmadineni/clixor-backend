[Unit]
# UGOS mounts /volume1 asynchronously. Docker's data-root lives on that volume,
# so wait for the storage manager to report ready before starting dockerd.
Requires=storage_serv.service
After=storage_serv.service
