global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: clustr-api
    scheme: http
    metrics_path: /metrics
    bearer_token_file: /run/secrets/metrics.token
    static_configs:
      - targets: ["api-a:8080", "api-b:8080"]
